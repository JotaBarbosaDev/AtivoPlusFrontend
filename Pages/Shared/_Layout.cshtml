<!DOCTYPE html>
<html lang="pt" class="dark">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - AtivosPlus</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f0f9ff',
                            100: '#e0f2fe',
                            200: '#bae6fd',
                            300: '#7dd3fc',
                            400: '#38bdf8',
                            500: '#0ea5e9',
                            600: '#0284c7',
                            700: '#0369a1',
                            800: '#075985',
                            900: '#0c4a6e',
                            950: '#082f49'
                        },
                        gray: {
                            50: '#f8fafc',
                            100: '#f1f5f9',
                            200: '#e2e8f0',
                            300: '#cbd5e1',
                            400: '#94a3b8',
                            500: '#64748b',
                            600: '#475569',
                            700: '#334155',
                            800: '#1e293b',
                            900: '#0f172a',
                            950: '#020617'
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                    },
                    animation: {
                        'fade-in': 'fadeIn 0.5s ease-in-out',
                        'slide-up': 'slideUp 0.3s ease-out',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0' },
                            '100%': { opacity: '1' },
                        },
                        slideUp: {
                            '0%': { transform: 'translateY(10px)', opacity: '0' },
                            '100%': { transform: 'translateY(0)', opacity: '1' },
                        }
                    },
                    boxShadow: {
                        'soft': '0 2px 8px -2px rgba(0, 0, 0, 0.2)',
                        'medium': '0 4px 16px -4px rgba(0, 0, 0, 0.3)',
                        'large': '0 8px 32px -8px rgba(0, 0, 0, 0.4)',
                    }
                }
            }
        };
    </script>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">

    <style>
        * {
            scrollbar-width: thin;
            scrollbar-color: #475569 #1e293b;
        }

        *::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        *::-webkit-scrollbar-track {
            background: #1e293b;
            border-radius: 3px;
        }

        *::-webkit-scrollbar-thumb {
            background: #475569;
            border-radius: 3px;
        }

        *::-webkit-scrollbar-thumb:hover {
            background: #64748b;
        }

        .glass-effect {
            background: rgba(15, 23, 42, 0.8);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(148, 163, 184, 0.1);
        }

        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .card-hover:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.2), 0 10px 10px -5px rgba(0, 0, 0, 0.1);
        }

        .gradient-text {
            background: linear-gradient(135deg, #0ea5e9, #06b6d4);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
    </style>
    @RenderSection("Styles", required: false)
</head>

<body class="bg-gray-950 text-gray-100 font-sans antialiased">
    <!-- Navigation -->
    <nav class="fixed top-0 z-50 w-full glass-effect border-b border-gray-800">
        <div class="px-6 py-4">
            <div class="flex items-center justify-between">
                <!-- Logo -->
                <div class="flex items-center space-x-3">
                    <div
                        class="w-8 h-8 bg-gradient-to-br from-primary-500 to-primary-600 rounded-lg flex items-center justify-center">
                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                        </svg>
                    </div>
                    <span class="text-xl font-bold gradient-text">AtivosPlus</span>
                </div>

                <!-- Navigation Links -->
                <div class="hidden md:flex items-center space-x-1">
                    <a href="/Geral"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Geral") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
                        </svg>
                        Dashboard
                    </a>

                    <a href="/Ativos"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Ativos") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z">
                            </path>
                        </svg>
                        Ativos
                    </a>

                    <!-- Asset Types Dropdown -->
                    <div class="relative group">
                        <button
                            class="px-4 py-2 rounded-lg text-sm font-medium transition-colors text-gray-300 hover:text-white hover:bg-gray-800 flex items-center">
                            <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z">
                                </path>
                            </svg>
                            Categorias
                            <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </button>
                        <div
                            class="absolute left-0 mt-2 w-64 glass-effect rounded-lg shadow-lg border border-gray-700 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
                            <div class="py-2">
                                <a href="/FundosInvestimento"
                                    class="block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-800 transition-colors">
                                    <svg class="w-4 h-4 inline mr-3" fill="none" stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                                    </svg>
                                    Fundos de Investimento
                                </a>
                                <a href="/ImovelArrendado"
                                    class="block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-800 transition-colors">
                                    <svg class="w-4 h-4 inline mr-3" fill="none" stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6">
                                        </path>
                                    </svg>
                                    Imóveis Arrendados
                                </a>
                                <a href="/DepositoPrazo"
                                    class="block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-800 transition-colors">
                                    <svg class="w-4 h-4 inline mr-3" fill="none" stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1">
                                        </path>
                                    </svg>
                                    Depósitos a Prazo
                                </a>
                            </div>
                        </div>
                    </div>

                    <a href="/Carteira"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Carteira") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z">
                            </path>
                        </svg>
                        Carteiras
                    </a>
                    <a href="/Mercado"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Mercado") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M16 8v8m-4-5v5m-4-2v2m-2 4h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z">
                            </path>
                        </svg>
                        Mercado
                    </a>
                </div>

                <!-- User Menu -->
                <div class="flex items-center space-x-4">
                    <!-- Profile Dropdown -->
                    <div class="relative group">
                        <button class="flex items-center space-x-2 p-2 rounded-lg hover:bg-gray-800 transition-colors">
                            <img class="w-8 h-8 rounded-full ring-2 ring-gray-700"
                                src="https://ui-avatars.com/api/?name=User&background=0ea5e9&color=fff" alt="Avatar">
                            <div class="hidden md:block text-left">
                                <p class="text-sm font-medium text-white" id="userDisplayName">Utilizador</p>
                                <p class="text-xs text-gray-400">Conta Ativa</p>
                            </div>
                            <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </button>
                        
                        <!-- Dropdown Menu -->
                        <div class="absolute right-0 mt-2 w-64 glass-effect rounded-lg shadow-lg border border-gray-700 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
                            <div class="py-2">
                                <!-- User Info Header -->
                                <div class="px-4 py-3 border-b border-gray-700">
                                    <p class="text-sm font-medium text-white" id="dropdownUserName">Utilizador</p>
                                    <p class="text-xs text-gray-400" id="dropdownUserEmail">utilizador@email.com</p>
                                </div>
                                
                                <!-- Menu Items -->
                                <div class="py-1">
                                    <a href="#" onclick="abrirPerfilModal()" 
                                        class="flex items-center px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-800 transition-colors">
                                        <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                        </svg>
                                        Ver/Editar Perfil
                                    </a>
                                    
                                    <div class="border-t border-gray-700 my-1"></div>
                                    
                                    <button onclick="logout()" 
                                        class="flex items-center w-full px-4 py-2 text-sm text-red-400 hover:text-red-300 hover:bg-gray-800 transition-colors">
                                        <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                                        </svg>
                                        Logout
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Mobile menu button -->
                    <button class="md:hidden p-2 text-gray-400 hover:text-white hover:bg-gray-800 rounded-lg"
                        onclick="toggleMobileMenu()">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16M4 18h16"></path>
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Mobile Menu -->
            <div id="mobile-menu" class="hidden md:hidden mt-4 pb-4 border-t border-gray-700">
                <div class="flex flex-col space-y-2 pt-4">
                    <a href="/Geral"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Geral") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Dashboard
                    </a>

                    <a href="/Ativos"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Ativos") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Ativos
                    </a>

                    <!-- Mobile Asset Types -->
                    <div class="px-4 py-2 text-sm font-medium text-gray-400">
                        Categorias de Investimento
                    </div>
                    <a href="/FundosInvestimento"
                        class="px-6 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/FundosInvestimento") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Fundos de Investimento
                    </a>
                    <a href="/ImovelArrendado"
                        class="px-6 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/ImovelArrendado") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Imóveis Arrendados
                    </a>
                    <a href="/DepositoPrazo"
                        class="px-6 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/DepositoPrazo") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Depósitos a Prazo
                    </a>

                    <a href="/Carteira"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Carteira") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Carteiras
                    </a>
                    <a href="/Mercado"
                        class="px-4 py-2 rounded-lg text-sm font-medium transition-colors @(Context.Request.Path.Value.Contains("/Mercado") ? "bg-primary-600 text-white" : "text-gray-300 hover:text-white hover:bg-gray-800")">
                        Mercado
                    </a>
                    
                    <!-- Mobile User Menu -->
                    <div class="border-t border-gray-700 mt-4 pt-4">
                        <div class="px-4 py-2 text-sm font-medium text-gray-400">
                            Conta
                        </div>
                        <button onclick="abrirPerfilModal()" 
                            class="w-full text-left px-4 py-2 rounded-lg text-sm font-medium transition-colors text-gray-300 hover:text-white hover:bg-gray-800">
                            <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                            </svg>
                            Ver/Editar Perfil
                        </button>
                        <button onclick="logout()" 
                            class="w-full text-left px-4 py-2 rounded-lg text-sm font-medium transition-colors text-red-400 hover:text-red-300 hover:bg-gray-800">
                            <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                            </svg>
                            Logout
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="pt-20 min-h-screen">
        @RenderBody()
    </main>

    <!-- Modal Perfil do Usuário -->
    <div id="perfilModal" class="fixed inset-0 bg-black/60 backdrop-blur-sm items-center justify-center z-50 hidden">
        <div class="bg-gray-900/95 backdrop-blur-xl border border-gray-800 rounded-2xl shadow-2xl w-full max-w-2xl mx-4 max-h-[90vh] overflow-y-auto">
            <div class="p-6">
                <div class="flex items-center justify-between mb-6">
                    <h2 class="text-2xl font-bold text-white">Informações do Perfil</h2>
                    <button onclick="fecharPerfilModal()" class="text-gray-400 hover:text-white transition-colors">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                    </button>
                </div>

                <form id="perfilForm" class="space-y-6">
                    <!-- Avatar Section -->
                    <div class="text-center mb-6">
                        <img id="perfilAvatar" class="w-20 h-20 rounded-full ring-4 ring-primary-500 mx-auto mb-4"
                            src="https://ui-avatars.com/api/?name=User&background=0ea5e9&color=fff" alt="Avatar">
                        <p class="text-sm text-gray-400">Avatar do utilizador</p>
                    </div>

                    <!-- Informações Pessoais -->
                    <div class="bg-gray-800/50 rounded-xl p-6 border border-gray-700">
                        <h3 class="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                            <svg class="w-5 h-5 text-primary-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                            </svg>
                            Informações Pessoais
                        </h3>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label for="perfilNome" class="block text-sm font-medium text-gray-300 mb-2">Nome Completo</label>
                                <input type="text" id="perfilNome" name="nome" required
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilEmail" class="block text-sm font-medium text-gray-300 mb-2">Email</label>
                                <input type="email" id="perfilEmail" name="email" required
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilTelefone" class="block text-sm font-medium text-gray-300 mb-2">Telefone</label>
                                <input type="text" id="perfilTelefone" name="telefone"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilNif" class="block text-sm font-medium text-gray-300 mb-2">NIF</label>
                                <input type="text" id="perfilNif" name="nif"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div class="md:col-span-2">
                                <label for="perfilIban" class="block text-sm font-medium text-gray-300 mb-2">IBAN</label>
                                <input type="text" id="perfilIban" name="iban"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>
                        </div>
                    </div>

                    <!-- Informações de Morada -->
                    <div class="bg-gray-800/50 rounded-xl p-6 border border-gray-700">
                        <h3 class="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                            <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            </svg>
                            Informações de Morada
                        </h3>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div class="md:col-span-2">
                                <label for="perfilRua" class="block text-sm font-medium text-gray-300 mb-2">Rua</label>
                                <input type="text" id="perfilRua" name="rua"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilPiso" class="block text-sm font-medium text-gray-300 mb-2">Piso</label>
                                <input type="text" id="perfilPiso" name="piso"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilNumeroPorta" class="block text-sm font-medium text-gray-300 mb-2">Número da Porta</label>
                                <input type="text" id="perfilNumeroPorta" name="numeroPorta"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilLocalidade" class="block text-sm font-medium text-gray-300 mb-2">Localidade</label>
                                <input type="text" id="perfilLocalidade" name="localidade"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilConcelho" class="block text-sm font-medium text-gray-300 mb-2">Concelho</label>
                                <input type="text" id="perfilConcelho" name="concelho"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilDistrito" class="block text-sm font-medium text-gray-300 mb-2">Distrito</label>
                                <input type="text" id="perfilDistrito" name="distrito"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>

                            <div>
                                <label for="perfilCodPostal" class="block text-sm font-medium text-gray-300 mb-2">Código Postal</label>
                                <input type="text" id="perfilCodPostal" name="codPostal"
                                    class="w-full bg-gray-800 border border-gray-700 rounded-xl px-4 py-3 text-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/25 transition-colors">
                            </div>
                        </div>
                    </div>

                    <div class="pt-4 border-t border-gray-700">
                        <div class="flex justify-end gap-3">
                            <button type="button" onclick="fecharPerfilModal()"
                                class="px-6 py-3 bg-gray-800 text-gray-300 rounded-xl hover:bg-gray-700 transition-colors duration-200 font-medium">
                                Cancelar
                            </button>
                            <button type="submit"
                                class="px-6 py-3 bg-primary-600 text-white rounded-xl hover:bg-primary-700 transition-colors duration-200 font-medium">
                                Guardar Alterações
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <!-- Authentication Script - Must load first -->
    <script src="~/js/auth.js"></script>

    <script>
        // Smooth scrolling
        document.documentElement.style.scrollBehavior = 'smooth';

        // Mobile menu toggle
        function toggleMobileMenu() {
            const mobileMenu = document.getElementById('mobile-menu');
            mobileMenu.classList.toggle('hidden');
        }

        // Profile Modal Functions
        function abrirPerfilModal() {
            const modal = document.getElementById('perfilModal');
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            carregarDadosUsuario();
        }

        function fecharPerfilModal() {
            const modal = document.getElementById('perfilModal');
            modal.classList.add('hidden');
            modal.classList.remove('flex');
        }

        // Load user data into profile modal
        async function carregarDadosUsuario() {
            try {
                const response = await fetch('/api/userinfo/getInfo?id=-1', {
                    method: 'GET',
                    credentials: 'include',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                if (response.ok) {
                    const data = await response.json();
                    const userData = data.userInfoRequest;
                    const moradaData = data.moradaRequest;
                    
                    // Update profile modal fields - Informações Pessoais
                    document.getElementById('perfilNome').value = userData.nome || '';
                    document.getElementById('perfilEmail').value = userData.email || '';
                    document.getElementById('perfilTelefone').value = userData.telefone || '';
                    document.getElementById('perfilNif').value = userData.nif || '';
                    document.getElementById('perfilIban').value = userData.iban || '';
                    
                    // Update profile modal fields - Morada
                    document.getElementById('perfilRua').value = moradaData.rua || '';
                    document.getElementById('perfilPiso').value = moradaData.piso || '';
                    document.getElementById('perfilNumeroPorta').value = moradaData.numeroPorta || '';
                    document.getElementById('perfilLocalidade').value = moradaData.localidade || '';
                    document.getElementById('perfilConcelho').value = moradaData.concelho || '';
                    document.getElementById('perfilDistrito').value = moradaData.distrito || '';
                    document.getElementById('perfilCodPostal').value = moradaData.codPostal || '';
                    
                    // Update avatar
                    const avatarUrl = `https://ui-avatars.com/api/?name=${encodeURIComponent(userData.nome || 'User')}&background=0ea5e9&color=fff`;
                    document.getElementById('perfilAvatar').src = avatarUrl;
                    
                    // Update dropdown display
                    document.getElementById('userDisplayName').textContent = userData.nome || 'Utilizador';
                    document.getElementById('dropdownUserName').textContent = userData.nome || 'Utilizador';
                    document.getElementById('dropdownUserEmail').textContent = userData.email || 'utilizador@email.com';
                    
                    // Update navigation avatar
                    const navAvatars = document.querySelectorAll('img[alt="Avatar"]');
                    navAvatars.forEach(avatar => {
                        avatar.src = avatarUrl;
                    });
                } else {
                    console.error('Erro ao carregar dados do usuário:', response.status);
                }
            } catch (error) {
                console.error('Erro ao carregar dados do usuário:', error);
            }
        }

        // Handle profile form submission
        document.getElementById('perfilForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const requestData = {
                userInfoRequest: {
                    id: -1,
                    nome: document.getElementById('perfilNome').value,
                    email: document.getElementById('perfilEmail').value,
                    telefone: document.getElementById('perfilTelefone').value,
                    morada_id: 0,
                    nif: document.getElementById('perfilNif').value,
                    dateTime: new Date().toISOString(),
                    iban: document.getElementById('perfilIban').value
                },
                moradaRequest: {
                    rua: document.getElementById('perfilRua').value,
                    piso: document.getElementById('perfilPiso').value,
                    numeroPorta: document.getElementById('perfilNumeroPorta').value,
                    concelho: document.getElementById('perfilConcelho').value,
                    distrito: document.getElementById('perfilDistrito').value,
                    localidade: document.getElementById('perfilLocalidade').value,
                    codPostal: document.getElementById('perfilCodPostal').value
                }
            };

            try {
                const response = await fetch('/api/userinfo/setInfo', {
                    method: 'PUT',
                    credentials: 'include',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(requestData)
                });

                if (response.ok) {
                    showToast('Perfil atualizado com sucesso!', 'success');
                    fecharPerfilModal();
                    // Reload user data to update UI
                    carregarDadosUsuario();
                } else {
                    showToast('Erro ao atualizar perfil', 'error');
                }
            } catch (error) {
                console.error('Erro ao atualizar perfil:', error);
                showToast('Erro ao atualizar perfil', 'error');
            }
        });

        // Logout function
        async function logout() {
            try {
                const response = await fetch('/api/auth/logout', {
                    method: 'POST',
                    credentials: 'include',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                // Clear auth cookies regardless of response
                clearAuthCookies();
                
                // Redirect to login page
                window.location.href = '/';
            } catch (error) {
                console.error('Erro durante logout:', error);
                // Still redirect even if logout fails
                clearAuthCookies();
                window.location.href = '/';
            }
        }

        // Toast notification function
        function showToast(message, type = 'info') {
            // Create toast element
            const toast = document.createElement('div');
            toast.className = `fixed top-4 right-4 px-6 py-4 rounded-xl shadow-lg transform transition-transform duration-300 z-50 ${
                type === 'success' ? 'bg-green-600' : 
                type === 'error' ? 'bg-red-600' : 'bg-blue-600'
            } text-white`;
            
            toast.innerHTML = `
                <div class="flex items-center gap-3">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        ${type === 'success' ? 
                            '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>' :
                            type === 'error' ?
                            '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>' :
                            '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>'
                        }
                    </svg>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(toast);
            
            // Show toast
            setTimeout(() => {
                toast.style.transform = 'translateX(0)';
            }, 100);
            
            // Hide toast after 3 seconds
            setTimeout(() => {
                toast.style.transform = 'translateX(100%)';
                setTimeout(() => {
                    document.body.removeChild(toast);
                }, 300);
            }, 3000);
        }

        // Load user data on page load
        document.addEventListener('DOMContentLoaded', function () {
            // Load user info for navigation display
            carregarDadosUsuario();
            
            // Animate cards on load
            const cards = document.querySelectorAll('.card-hover');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';

                setTimeout(() => {
                    card.style.transition = 'all 0.5s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100);
            });
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>

</html>
