@page
@model AtivoPlusFrontend.Pages.Pages_FundosInvestimento
@{
  ViewData["Title"] = "Fundos de Investimento";
  Layout = "_Layout";
}

<div class="min-h-screen bg-gray-950 pt-20 pb-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

    <!-- Header Section -->
    <div class="flex justify-between items-center mb-8">
      <div>
        <h1 class="text-4xl font-bold bg-gradient-to-r from-primary-400 to-blue-400 bg-clip-text text-transparent mb-2">
          Fundos de Investimento
        </h1>
        <p class="text-gray-400">Gerencie seus investimentos em fundos e criptomoedas</p>
      </div>
      <button id="novoFundoBtn"
        class="bg-gradient-to-r from-primary-600 to-blue-600 text-white px-6 py-3 rounded-xl hover:from-primary-700 hover:to-blue-700 focus:ring-4 focus:ring-primary-500/25 transition-all duration-200 transform hover:scale-105 active:scale-95 flex items-center gap-2 font-semibold shadow-lg">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
        </svg>
        Adicionar Fundo
      </button>
    </div>

    <!-- Summary Cards -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl p-6">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h3 class="text-lg font-semibold text-white">Total Investido</h3>
            <p class="text-gray-400 text-sm">Em fundos</p>
          </div>
          <div class="w-12 h-12 bg-primary-500/10 rounded-xl flex items-center justify-center">
            <svg class="w-6 h-6 text-primary-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
        </div>
        <div class="flex items-center gap-2">
          <span id="totalInvestido" class="text-2xl font-bold text-white">€2.500,00</span>
        </div>
      </div>

      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl p-6">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h3 class="text-lg font-semibold text-white">Valor Atual</h3>
            <p class="text-gray-400 text-sm">Market value</p>
          </div>
          <div class="w-12 h-12 bg-green-500/10 rounded-xl flex items-center justify-center">
            <svg class="w-6 h-6 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
            </svg>
          </div>
        </div>
        <div class="flex items-center gap-2">
          <span id="valorAtual" class="text-2xl font-bold text-green-400">€2.750,00</span>
          <span class="text-green-400 text-sm">+10%</span>
        </div>
      </div>

      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl p-6">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h3 class="text-lg font-semibold text-white">Retorno</h3>
            <p class="text-gray-400 text-sm">Ganho/Perda</p>
          </div>
          <div class="w-12 h-12 bg-yellow-500/10 rounded-xl flex items-center justify-center">
            <svg class="w-6 h-6 text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M7 11l5-5m0 0l5 5m-5-5v12" />
            </svg>
          </div>
        </div>
        <div class="flex items-center gap-2">
          <span id="retorno" class="text-2xl font-bold text-yellow-400">€250,00</span>
        </div>
      </div>

      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl p-6">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h3 class="text-lg font-semibold text-white">Total Fundos</h3>
            <p class="text-gray-400 text-sm">Quantidade</p>
          </div>
          <div class="w-12 h-12 bg-blue-500/10 rounded-xl flex items-center justify-center">
            <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
          </div>
        </div>
        <div class="flex items-center gap-2">
          <span id="totalFundos" class="text-2xl font-bold text-white">3</span>
          <span class="text-blue-400 text-sm">fundos</span>
        </div>
      </div>
    </div>

    <!-- Fundos Grid -->
    <div id="fundosContainer" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      
      <!-- Demo Card 1 - Bitcoin -->
      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl overflow-hidden hover:border-primary-500/30 transition-all duration-300 group">
        <div class="relative h-32 bg-gradient-to-br from-orange-500 to-yellow-500 p-6">
          <div class="absolute top-4 right-4">
            <span class="text-2xl">₿</span>
          </div>
          <div class="absolute bottom-4 left-6">
            <h3 class="text-xl font-bold text-white">Bitcoin</h3>
            <span class="text-white/80 text-sm">BTC/USD</span>
          </div>
        </div>
        
        <div class="p-6">
          <div class="space-y-3 mb-4">
            <div class="flex justify-between">
              <span class="text-gray-400">Investido:</span>
              <span class="text-white font-semibold">€500,00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Valor Atual:</span>
              <span class="text-green-400 font-semibold">€650,00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Retorno:</span>
              <span class="text-green-400 font-semibold">+30%</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Data:</span>
              <span class="text-white text-sm">30/05/2025</span>
            </div>
          </div>
          
          <button onclick="abrirModalDetalhes('btc')"
            class="w-full py-2 bg-primary-600/20 hover:bg-primary-600/30 text-primary-400 rounded-lg transition-colors duration-200 text-sm font-medium">
            Ver Detalhes
          </button>
        </div>
      </div>

      <!-- Demo Card 2 - Ethereum -->
      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl overflow-hidden hover:border-primary-500/30 transition-all duration-300 group">
        <div class="relative h-32 bg-gradient-to-br from-blue-500 to-purple-500 p-6">
          <div class="absolute top-4 right-4">
            <span class="text-2xl">Ξ</span>
          </div>
          <div class="absolute bottom-4 left-6">
            <h3 class="text-xl font-bold text-white">Ethereum</h3>
            <span class="text-white/80 text-sm">ETH/USD</span>
          </div>
        </div>
        
        <div class="p-6">
          <div class="space-y-3 mb-4">
            <div class="flex justify-between">
              <span class="text-gray-400">Investido:</span>
              <span class="text-white font-semibold">€1.000,00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Valor Atual:</span>
              <span class="text-green-400 font-semibold">€1.150,00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Retorno:</span>
              <span class="text-green-400 font-semibold">+15%</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Data:</span>
              <span class="text-white text-sm">25/05/2025</span>
            </div>
          </div>
          
          <button onclick="abrirModalDetalhes('eth')"
            class="w-full py-2 bg-primary-600/20 hover:bg-primary-600/30 text-primary-400 rounded-lg transition-colors duration-200 text-sm font-medium">
            Ver Detalhes
          </button>
        </div>
      </div>

      <!-- Demo Card 3 - S&P 500 -->
      <div class="bg-gray-900/50 backdrop-blur-xl border border-gray-800 rounded-2xl overflow-hidden hover:border-primary-500/30 transition-all duration-300 group">
        <div class="relative h-32 bg-gradient-to-br from-green-500 to-teal-500 p-6">
          <div class="absolute top-4 right-4">
            <span class="text-2xl">📈</span>
          </div>
          <div class="absolute bottom-4 left-6">
            <h3 class="text-xl font-bold text-white">S&P 500 ETF</h3>
            <span class="text-white/80 text-sm">SPY</span>
          </div>
        </div>
        
        <div class="p-6">
          <div class="space-y-3 mb-4">
            <div class="flex justify-between">
              <span class="text-gray-400">Investido:</span>
              <span class="text-white font-semibold">€1.000,00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Valor Atual:</span>
              <span class="text-green-400 font-semibold">€950,00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Retorno:</span>
              <span class="text-red-400 font-semibold">-5%</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-400">Data:</span>
              <span class="text-white text-sm">20/05/2025</span>
            </div>
          </div>
          
          <button onclick="abrirModalDetalhes('spy')"
            class="w-full py-2 bg-primary-600/20 hover:bg-primary-600/30 text-primary-400 rounded-lg transition-colors duration-200 text-sm font-medium">
            Ver Detalhes
          </button>
        </div>
      </div>

    </div>

    <!-- Empty State (Hidden by default since we have demo data) -->
    <div id="emptyState" class="text-center py-16 hidden">
      <div class="w-24 h-24 bg-gray-800 rounded-full flex items-center justify-center mx-auto mb-4">
        <svg class="w-12 h-12 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
        </svg>
      </div>
      <h3 class="text-xl font-semibold text-gray-400 mb-2">Nenhum fundo encontrado</h3>
      <p class="text-gray-500 mb-6">Adicione seu primeiro fundo de investimento</p>
      <button onclick="abrirModal()"
        class="bg-gradient-to-r from-primary-600 to-blue-600 text-white px-6 py-3 rounded-xl hover:from-primary-700 hover:to-blue-700 transition-all duration-200">
        Adicionar Primeiro Fundo
      </button>
    </div>
  </div>

  <!-- Modal Adicionar Fundo -->
  <div id="novoFundoModal"
    class="fixed inset-0 bg-black/60 backdrop-blur-sm items-center justify-center z-50 hidden">
    <div
      class="bg-gray-900/95 backdrop-blur-xl border border-gray-800 rounded-2xl p-8 shadow-2xl w-full max-w-md mx-4 transform transition-all duration-300">
      <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl font-bold text-white">Adicionar Fundo</h2>
        <button id="fecharModalBtn" class="text-gray-400 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>

      <form id="formNovoFundo" class="space-y-6">
        <div>
          <label for="nomeFundo" class="block text-sm font-medium text-gray-300 mb-2">
            Nome do Fundo
          </label>
          <input type="text" id="nomeFundo" required
            class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-xl text-white placeholder-gray-400 focus:border-primary-500 focus:ring-2 focus:ring-primary-500/20 transition-all duration-200"
            placeholder="Ex: Bitcoin, S&P 500">
        </div>

        <div>
          <label for="ativoSigla" class="block text-sm font-medium text-gray-300 mb-2">
            Sigla do Ativo
          </label>
          <input type="text" id="ativoSigla" required
            class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-xl text-white placeholder-gray-400 focus:border-primary-500 focus:ring-2 focus:ring-primary-500/20 transition-all duration-200"
            placeholder="Ex: BTC/USD, SPY">
        </div>

        <div>
          <label for="montanteInvestido" class="block text-sm font-medium text-gray-300 mb-2">
            Montante Investido (€)
          </label>
          <input type="number" id="montanteInvestido" required step="0.01"
            class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-xl text-white placeholder-gray-400 focus:border-primary-500 focus:ring-2 focus:ring-primary-500/20 transition-all duration-200"
            placeholder="0.00">
        </div>

        <div class="flex gap-3 pt-4">
          <button type="button" id="cancelarBtn"
            class="flex-1 py-3 bg-gray-800 text-gray-300 rounded-xl hover:bg-gray-700 transition-colors duration-200">
            Cancelar
          </button>
          <button type="submit"
            class="flex-1 py-3 bg-gradient-to-r from-primary-600 to-blue-600 text-white rounded-xl hover:from-primary-700 hover:to-blue-700 transition-all duration-200">
            Adicionar Fundo
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Toast Notifications -->
  <div id="toast-container" class="fixed bottom-4 right-4 z-50 space-y-2"></div>
</div>

<script>
  // Toast notification function
  function showToast(message, type = "info") {
    const toast = document.createElement("div");
    let iconSVG, bgClass;

    switch (type) {
      case "success":
        bgClass = "bg-green-500/10 border-green-500/20";
        iconSVG = `<svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414L8.414 15l-4.121-4.121a1 1 0 111.414-1.414L8.414 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
    </svg>`;
        break;
      case "error":
        bgClass = "bg-red-500/10 border-red-500/20";
        iconSVG = `<svg class="w-5 h-5 text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
    </svg>`;
        break;
      default:
        bgClass = "bg-primary-500/10 border-primary-500/20";
        iconSVG = `<svg class="w-5 h-5 text-primary-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
    </svg>`;
    }

    toast.className = `flex items-center p-4 space-x-3 ${bgClass} border backdrop-blur-xl rounded-xl shadow-lg text-white animate-slide-up`;
    toast.innerHTML = `${iconSVG}<div class="text-sm font-medium">${message}</div>`;

    document.getElementById("toast-container").appendChild(toast);

    setTimeout(() => {
      toast.classList.add("opacity-0", "transition-opacity", "duration-300");
      setTimeout(() => toast.remove(), 300);
    }, 3000);
  }

  // Modal functions
  function abrirModal() {
    const modal = document.getElementById('novoFundoModal');
    modal.classList.remove('hidden');
    modal.classList.add('flex');
    document.getElementById('nomeFundo').focus();
  }

  function fecharModal() {
    const modal = document.getElementById('novoFundoModal');
    modal.classList.add('hidden');
    modal.classList.remove('flex');
    document.getElementById('formNovoFundo').reset();
  }

  function abrirModalDetalhes(fundoId) {
    showToast(`Detalhes do fundo ${fundoId} - Em desenvolvimento`, "info");
  }

  // Event listeners
  document.addEventListener('DOMContentLoaded', function () {
    // Modal event listeners
    document.getElementById('novoFundoBtn').addEventListener('click', abrirModal);
    document.getElementById('fecharModalBtn').addEventListener('click', fecharModal);
    document.getElementById('cancelarBtn').addEventListener('click', fecharModal);

    // Form submission
    document.getElementById('formNovoFundo').addEventListener('submit', async function (e) {
      e.preventDefault();

      const nome = document.getElementById('nomeFundo').value.trim();
      const sigla = document.getElementById('ativoSigla').value.trim();
      const montante = document.getElementById('montanteInvestido').value;

      if (!nome || !sigla || !montante) {
        showToast("Por favor, preencha todos os campos.", "error");
        return;
      }

      // Simulated API call - replace with actual API integration later
      try {
        showToast("Fundo adicionado com sucesso! (Demo)", "success");
        fecharModal();
        // Here you would call carregarFundos() after API integration
      } catch (error) {
        showToast("Erro ao adicionar fundo.", "error");
      }
    });

    // Close modal when clicking outside
    document.getElementById('novoFundoModal').addEventListener('click', function (e) {
      if (e.target === this) fecharModal();
    });
  });
</script>
