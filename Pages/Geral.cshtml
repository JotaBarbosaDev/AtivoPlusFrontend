@page
@model AtivoPlusFrontend.Pages.Pages_Geral
@{
    ViewData["Title"] = "Geral";
    Layout = "_Layout";
}

<main class="min-h-screen bg-gradient-to-br from-black via-gray-950 to-slate-950 pt-6 px-4 relative overflow-hidden">
    <!-- Enhanced Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-950/20 via-indigo-950/15 to-cyan-950/20"></div>
    <div class="absolute top-1/6 left-1/6 w-[500px] h-[500px] bg-blue-600/10 rounded-full blur-3xl animate-pulse">
    </div>
    <div class="absolute bottom-1/6 right-1/6 w-[500px] h-[500px] bg-blue-600/10 rounded-full blur-3xl animate-pulse"
        style="animation-delay: 2s"></div>
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[300px] h-[300px] bg-cyan-600/8 rounded-full blur-3xl animate-pulse"
        style="animation-delay: 4s"></div>

    <!-- Animated particles -->
    <div class="absolute top-20 left-20 w-2 h-2 bg-blue-400/30 rounded-full animate-bounce" style="animation-delay: 1s">
    </div>
    <div class="absolute top-40 right-32 w-1 h-1 bg-cyan-400/40 rounded-full animate-ping" style="animation-delay: 3s">
    </div>
    <div class="absolute bottom-32 left-40 w-1.5 h-1.5 bg-indigo-400/35 rounded-full animate-pulse"
        style="animation-delay: 2s"></div>

    <!-- Hero Section with enhanced gradient -->
    <div class="relative mb-12 z-10">
        <div class="absolute inset-0 bg-gradient-to-r from-blue-600/40 to-cyan-500/40 rounded-3xl blur-3xl"></div>
        <div class="relative dark-glass-card p-10 rounded-3xl border border-blue-500/30">
            <div class="text-center">
                <h1
                    class="text-6xl font-black bg-gradient-to-r from-blue-300 via-indigo-300 to-cyan-300 bg-clip-text text-transparent mb-4 tracking-tight">
                    💎 AtivoPlus
                </h1>
                <p class="text-gray-200 text-xl font-medium mb-4">Visão completa dos seus investimentos e performance em
                    tempo real</p>
                <div class="mt-6 flex justify-center">
                    <div class="flex space-x-3">
                        <div class="w-3 h-3 bg-blue-400 rounded-full animate-pulse shadow-lg shadow-blue-400/50"></div>
                        <div class="w-3 h-3 bg-indigo-400 rounded-full animate-pulse shadow-lg shadow-indigo-400/50"
                            style="animation-delay: 0.2s"></div>
                        <div class="w-3 h-3 bg-cyan-400 rounded-full animate-pulse shadow-lg shadow-cyan-400/50"
                            style="animation-delay: 0.4s"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Key Metrics Cards -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8 relative z-10">
        <!-- Lucro Total Card -->
        <div
            class="dark-glass-card group hover:scale-105 hover:-translate-y-2 transition-all duration-500 p-6 rounded-2xl border border-emerald-500/20 relative overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-br from-emerald-500/5 to-green-500/5"></div>
            <div class="relative z-10">
                <div class="flex items-center justify-between mb-4">
                    <div class="p-3 rounded-xl bg-gradient-to-r from-emerald-500/30 to-green-500/30 backdrop-blur-sm">
                        <svg class="w-6 h-6 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1">
                            </path>
                        </svg>
                    </div>
                    <span id="profitPercentage"
                        class="text-xs font-semibold text-emerald-300 bg-emerald-500/20 px-3 py-1 rounded-full border border-emerald-500/30">
                        +0.0%
                    </span>
                </div>
                <h3 class="text-sm font-medium text-gray-400 mb-2">💰 Lucro Total</h3>
                <p id="totalProfit" class="text-4xl font-black text-white mb-2 font-mono">€0</p>
                <p class="text-sm text-emerald-400 font-medium">Atualizado automaticamente</p>
            </div>
        </div>

        <!-- Depósitos Card -->
        <div
            class="dark-glass-card group hover:scale-105 hover:-translate-y-2 transition-all duration-500 p-6 rounded-2xl border border-blue-500/20 relative overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 to-cyan-500/5"></div>
            <div class="relative z-10">
                <div class="flex items-center justify-between mb-4">
                    <div class="p-3 rounded-xl bg-gradient-to-r from-blue-500/30 to-cyan-500/30 backdrop-blur-sm">
                        <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4">
                            </path>
                        </svg>
                    </div>
                    <span id="depositosCount"
                        class="text-xs font-semibold text-blue-300 bg-blue-500/20 px-3 py-1 rounded-full border border-blue-500/30">
                        0
                    </span>
                </div>
                <h3 class="text-sm font-medium text-gray-400 mb-2">🏦 Depósitos a Prazo</h3>
                <p id="depositosTotal" class="text-4xl font-black text-white mb-2 font-mono">€0</p>
                <p class="text-sm text-blue-400 font-medium">Renda fixa garantida</p>
            </div>
        </div>

        <!-- Fundos Card -->
        <div
            class="dark-glass-card group hover:scale-105 hover:-translate-y-2 transition-all duration-500 p-6 rounded-2xl border border-blue-500/20 relative overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 to-indigo-500/5"></div>
            <div class="relative z-10">
                <div class="flex items-center justify-between mb-4">
                    <div class="p-3 rounded-xl bg-gradient-to-r from-blue-500/30 to-indigo-500/30 backdrop-blur-sm">
                        <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                        </svg>
                    </div>
                    <span id="fundosCount"
                        class="text-xs font-semibold text-blue-300 bg-blue-500/20 px-3 py-1 rounded-full border border-blue-500/30">
                        0
                    </span>
                </div>
                <h3 class="text-sm font-medium text-gray-400 mb-2">📈 Fundos de Investimento</h3>
                <p id="fundosTotal" class="text-4xl font-black text-white mb-2 font-mono">€0</p>
                <p class="text-sm text-blue-400 font-medium">Diversificação inteligente</p>
            </div>
        </div>

        <!-- Imóveis Card -->
        <div
            class="dark-glass-card group hover:scale-105 hover:-translate-y-2 transition-all duration-500 p-6 rounded-2xl border border-orange-500/20 relative overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-br from-orange-500/5 to-yellow-500/5"></div>
            <div class="relative z-10">
                <div class="flex items-center justify-between mb-4">
                    <div class="p-3 rounded-xl bg-gradient-to-r from-orange-500/30 to-yellow-500/30 backdrop-blur-sm">
                        <svg class="w-6 h-6 text-orange-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6">
                            </path>
                        </svg>
                    </div>
                    <span id="imoveisCount"
                        class="text-xs font-semibold text-orange-300 bg-orange-500/20 px-3 py-1 rounded-full border border-orange-500/30">
                        0
                    </span>
                </div>
                <h3 class="text-sm font-medium text-gray-400 mb-2">🏠 Imóveis</h3>
                <p id="imoveisTotal" class="text-4xl font-black text-white mb-2 font-mono">€0</p>
                <p class="text-sm text-orange-400 font-medium">Patrimônio sólido</p>
            </div>
        </div>
    </div>

    <!-- Risk Analysis Section -->
    <div class="glass-card p-8 rounded-3xl mb-8">
        <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-8">
            <div>
                <h2 class="text-3xl font-bold text-white mb-2">Resumo por Tipo de Investimento</h2>
                <p class="text-gray-400">Distribuição dos seus investimentos por categoria</p>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <!-- Depositos Card -->
            <div class="glass-card p-6 rounded-2xl group hover:scale-105 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold text-white">Depósitos a Prazo</h3>
                    <div class="p-2 rounded-lg bg-blue-500/20">
                        <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1">
                            </path>
                        </svg>
                    </div>
                </div>

                <div class="relative mb-3">
                    <div class="flex items-center justify-between mb-2">
                        <span id="depositosTotal2" class="text-3xl font-bold text-white loading-indicator">€0</span>
                        <span id="depositosCount2" class="text-sm text-blue-400 font-medium loading-indicator">0
                            ativos</span>
                    </div>
                </div>

                <p class="text-sm text-gray-400">Investimentos de renda fixa</p>
            </div>

            <!-- Fundos Card -->
            <div class="glass-card p-6 rounded-2xl group hover:scale-105 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold text-white">Fundos de Investimento</h3>
                    <div class="p-2 rounded-lg bg-blue-500/20">
                        <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                        </svg>
                    </div>
                </div>

                <div class="relative mb-3">
                    <div class="flex items-center justify-between mb-2">
                        <span id="fundosTotal2" class="text-3xl font-bold text-white loading-indicator">€0</span>
                        <span id="fundosCount2" class="text-sm text-blue-400 font-medium loading-indicator">0
                            ativos</span>
                    </div>
                </div>

                <p class="text-sm text-gray-400">Fundos e ETFs</p>
            </div>

            <!-- Imoveis Card -->
            <div class="glass-card p-6 rounded-2xl group hover:scale-105 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold text-white">Imóveis</h3>
                    <div class="p-2 rounded-lg bg-green-500/20">
                        <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4">
                            </path>
                        </svg>
                    </div>
                </div>

                <div class="relative mb-3">
                    <div class="flex items-center justify-between mb-2">
                        <span id="imoveisTotal2" class="text-3xl font-bold text-white loading-indicator">€0</span>
                        <span id="imoveisCount2" class="text-sm text-green-400 font-medium loading-indicator">0
                            ativos</span>
                    </div>
                </div>

                <p class="text-sm text-gray-400">Propriedades arrendadas</p>
            </div>
        </div>
    </div>

    <!-- Top Assets Section -->
    <div class="mb-8">
        <div class="glass-card p-6 rounded-2xl">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-xl font-bold text-white">Top 5 Ativos</h2>
                <button onclick="DashboardManager.refreshDashboard()"
                    class="text-primary-400 hover:text-primary-300 transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15">
                        </path>
                    </svg>
                </button>
            </div>
            <div id="topAssetsContainer" class="space-y-4">
                <div class="loading-indicator">
                    <div class="flex justify-center items-center p-8">
                        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-primary-400"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

@section Scripts {
    <!-- Include calculation and dashboard scripts -->
    <script src="~/js/ativosCalculos.js"></script>
    <script src="~/js/geral.js"></script>

    <script>
        // Enhanced CSS for dark glass effect
        document.addEventListener('DOMContentLoaded', function () {
            const style = document.createElement('style');
            style.textContent = `
                                        .dark-glass-card {
                                            background: rgba(3, 7, 18, 0.4);
                                            backdrop-filter: blur(25px);
                                            border: 1px solid rgba(255, 255, 255, 0.03);
                                            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.8);
                                        }
                
                                        .dark-glass-card:hover {
                                            background: rgba(3, 7, 18, 0.6);
                                            border: 1px solid rgba(255, 255, 255, 0.08);
                                            box-shadow: 0 35px 70px -12px rgba(0, 0, 0, 0.9);
                                            transform: translateY(-8px);
                                        }

                                        .glass-card {
                                            background: rgba(3, 7, 18, 0.25);
                                            backdrop-filter: blur(20px);
                                            border: 1px solid rgba(255, 255, 255, 0.02);
                                            box-shadow: 0 20px 40px -12px rgba(0, 0, 0, 0.7);
                                        }
                
                                        .glass-card:hover {
                                            background: rgba(3, 7, 18, 0.4);
                                            border: 1px solid rgba(255, 255, 255, 0.06);
                                            transform: translateY(-4px);
                                            box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.8);
                                        }
                
                                        /* Loading animations */
                                        .loading-indicator {
                                            opacity: 0.7;
                                            animation: pulse 2s infinite;
                                        }
                
                                        @@keyframes pulse {
                                            0%, 100% { opacity: 0.7; }
                                            50% { opacity: 1; }
                                        }
                
                                        /* Gradient text animation */
                                        .bg-gradient-to-r {
                                            background-size: 200% 200%;
                                            animation: gradient 3s ease infinite;
                                        }
                
                                        @@keyframes gradient {
                                            0% { background-position: 0% 50%; }
                                            50% { background-position: 100% 50%; }
                                            100% { background-position: 0% 50%; }
                                        }

                                        /* Floating particles effect */
                                        @@keyframes float {
                                            0%, 100% { transform: translateY(0px); }
                                            50% { transform: translateY(-20px); }
                                        }

                                        .floating-particle {
                                            animation: float 6s ease-in-out infinite;
                                        }
                
                                        /* Auto-refresh indicator */
                                        .refresh-indicator {
                                            position: fixed;
                                            top: 120px;
                                            right: 20px;
                                            background: linear-gradient(135deg, #10b981, #059669);
                                            color: white;
                                            padding: 12px 20px;
                                            border-radius: 25px;
                                            font-size: 14px;
                                            font-weight: 600;
                                            z-index: 1000;
                                            transform: translateX(120%);
                                            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
                                            box-shadow: 0 10px 25px rgba(16, 185, 129, 0.3);
                                        }
                
                                        .refresh-indicator.show {
                                            transform: translateX(0);
                                        }

                                        /* Enhanced glowing effects */
                                        .glow-emerald {
                                            box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
                                        }

                                        .glow-blue {
                                            box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
                                        }

                                        .glow-blue2 {
                                            box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
                                        }

                                        .glow-orange {
                                            box-shadow: 0 0 20px rgba(249, 115, 22, 0.3);
                                        }

                                        /* Neon card effects */
                                        .neon-emerald {
                                            box-shadow: 
                                                0 0 5px rgba(16, 185, 129, 0.4),
                                                0 0 25px rgba(16, 185, 129, 0.2),
                                                0 0 50px rgba(16, 185, 129, 0.1);
                                        }

                                        .neon-blue {
                                            box-shadow: 
                                                0 0 5px rgba(59, 130, 246, 0.4),
                                                0 0 25px rgba(59, 130, 246, 0.2),
                                                0 0 50px rgba(59, 130, 246, 0.1);
                                        }

                                        .neon-indigo {
                                            box-shadow: 
                                                0 0 5px rgba(99, 102, 241, 0.4),
                                                0 0 25px rgba(99, 102, 241, 0.2),
                                                0 0 50px rgba(99, 102, 241, 0.1);
                                        }

                                        .neon-orange {
                                            box-shadow: 
                                                0 0 5px rgba(249, 115, 22, 0.4),
                                                0 0 25px rgba(249, 115, 22, 0.2),
                                                0 0 50px rgba(249, 115, 22, 0.1);
                                        }

                                        /* Enhanced dark glass cards with neon effects */
                                        .dark-glass-card:hover .neon-emerald,
                                        .dark-glass-card:hover .neon-blue,
                                        .dark-glass-card:hover .neon-indigo,
                                        .dark-glass-card:hover .neon-orange {
                                            box-shadow: 
                                                0 0 10px rgba(var(--glow-color), 0.6),
                                                0 0 40px rgba(var(--glow-color), 0.4),
                                                0 0 80px rgba(var(--glow-color), 0.2);
                                        }
                                    `;
            document.head.appendChild(style);

            // Initialize dashboard with debugging
            console.log('🚀 Initializing dashboard...');

            // Force initialization after a small delay to ensure all scripts are loaded
            setTimeout(() => {
                if (window.DashboardManager && typeof window.DashboardManager.initDashboard === 'function') {
                    console.log('✅ DashboardManager found, initializing...');
                    window.DashboardManager.initDashboard();
                } else {
                    console.error('❌ DashboardManager not found. Available:', Object.keys(window));
                    // Try direct initialization
                    if (typeof initDashboard === 'function') {
                        console.log('🔄 Trying direct initDashboard...');
                        initDashboard();
                    }
                }
            }, 500);

            // Show auto-refresh indicator
            const showRefreshIndicator = () => {
                let indicator = document.querySelector('.refresh-indicator');
                if (!indicator) {
                    indicator = document.createElement('div');
                    indicator.className = 'refresh-indicator';
                    indicator.innerHTML = '✨ Dados atualizados com sucesso!';
                    document.body.appendChild(indicator);
                }

                indicator.classList.add('show');
                setTimeout(() => {
                    indicator.classList.remove('show');
                }, 3000);
            };

            // Override refresh function if available
            if (window.DashboardManager && window.DashboardManager.refreshDashboard) {
                const originalRefresh = window.DashboardManager.refreshDashboard;
                window.DashboardManager.refreshDashboard = async function () {
                    console.log('🔄 Refreshing dashboard...');
                    await originalRefresh();
                    showRefreshIndicator();
                };
            }

            // Add debug info
            console.log('🔍 Available functions:', {
                calcularLucroDeposito: typeof calcularLucroDeposito,
                calcularLucroFundoInvestimento: typeof calcularLucroFundoInvestimento,
                calcularLucroImovel: typeof calcularLucroImovel,
                initDashboard: typeof initDashboard,
                DashboardManager: typeof window.DashboardManager
            });
        });

        // Enhanced error handling with user feedback
        window.addEventListener('error', function (event) {
            console.error('🚨 Global error:', event.error);

            // Show user-friendly error notification
            const errorToast = document.createElement('div');
            errorToast.className = 'fixed top-4 left-4 z-50 bg-red-500 text-white px-6 py-4 rounded-lg shadow-lg';
            errorToast.innerHTML = '⚠️ Erro ao carregar dados. Tentando novamente...';
            document.body.appendChild(errorToast);

            setTimeout(() => {
                document.body.removeChild(errorToast);
            }, 5000);
        });

        // Add page visibility handling for performance
        document.addEventListener('visibilitychange', function () {
            if (document.visibilityState === 'visible') {
                console.log('👁️ Page visible, refreshing data...');
                if (window.DashboardManager && window.DashboardManager.refreshDashboard) {
                    setTimeout(() => {
                        window.DashboardManager.refreshDashboard();
                    }, 1000);
                }
            }
        });
    </script>
}
